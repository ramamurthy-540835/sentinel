# Saved Prompt ID: 3381323161097207808 - PRISM Coder Agent

## Source
Vertex AI Saved Prompt (GCS + local: ../coder/saved_prompts/3381323161097207808/)

## Core Mission
Build a reusable **PRISM Coder Agent** that can take any Vertex AI Saved Prompt ID and turn it into production code using Aider.

## Mandatory Capabilities (from prompt)
- Retrieve and process saved prompts from Vertex AI Agent Platform
- Deterministic extraction (preserve source content 100%, no LLM rewriting)
- Support chunking for large prompts
- Launch coding via `start_aider.sh`
- Work with Gemini 3.5 Flash as primary (Gemini 2.5 Flash as fallback)
- Use GCP-native services
- Structured logging and reporting
- Never hardcode prompt IDs
- Security: Never print tokens or secrets

## Key Technical Requirements
- Python 3.11+
- Config-driven via `models.json`
- Proper handling of Vertex AI Dataset format (raw.json, attachments)
- Output structure: `saved_prompts/<prompt_id>/` with specific files (raw.json, chunks, master.md, etc.)
- Integration with Aider using dynamic model routing from models.json

## Non-Functional Requirements
- Reproducible / deterministic behavior
- Clear error messages
- Production-quality code output

